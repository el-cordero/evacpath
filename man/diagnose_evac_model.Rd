% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{diagnose_evac_model}
\alias{diagnose_evac_model}
\title{Diagnose evacuation-model inputs}
\usage{
diagnose_evac_model(
  hazard_zone,
  roads,
  dem,
  target_crs = NULL,
  escape_zone = NULL,
  origins = NULL,
  destinations = NULL,
  conductance = NULL,
  check_reachability = TRUE,
  sample_size = 100,
  ...
)
}
\arguments{
\item{hazard_zone}{Hazard-zone polygon, raster, or file path.}

\item{roads}{Road/pathway line layer or file path.}

\item{dem}{Digital elevation model raster or file path.}

\item{target_crs}{Optional target coordinate reference system.}

\item{escape_zone}{Optional escape-boundary zone.}

\item{origins}{Optional modeled origin points.}

\item{destinations}{Optional escape/safety destination points.}

\item{conductance}{Optional \code{leastcostpath} conductance surface. When supplied
with origins and destinations, routing checks are run.}

\item{check_reachability}{Logical. Attempt sampled routing checks when routing
inputs are available.}

\item{sample_size}{Maximum number of origins used for sampled reachability
checks.}

\item{...}{Reserved for future extensions.}
}
\value{
An \code{evac_diagnostics} list with \code{issues}, \code{summary},
\code{diagnostic_layers}, and \code{metadata}.
}
\description{
Runs spatial quality assurance and quality control checks before or after
evacuation modeling. Unlike the modeling workflow, diagnostics collect issues
into a report so several input problems can be reviewed together.
}
\examples{
dem <- terra::rast(nrows = 5, ncols = 5, xmin = 0, xmax = 5, ymin = 0, ymax = 5,
  vals = 1, crs = "EPSG:3857")
hazard <- terra::as.polygons(dem, dissolve = TRUE)
roads <- terra::vect(matrix(c(0, 2.5, 5, 2.5), ncol = 2, byrow = TRUE),
  type = "lines", crs = "EPSG:3857")
diagnostics <- diagnose_evac_model(hazard, roads, dem)
diagnostics
has_errors(diagnostics)
}
