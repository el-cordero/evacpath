% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/planning_workflows.R
\name{compare_evac_scenarios}
\alias{compare_evac_scenarios}
\title{Compare evacuation scenarios}
\usage{
compare_evac_scenarios(
  scenarios,
  ...,
  scenario_id_col = "scenario",
  keep_routes = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{scenarios}{Named list. Each element is a named list of arguments that
override shared arguments passed through \code{...}.}

\item{...}{Shared arguments passed to \code{\link[=run_evacpath]{run_evacpath()}}.}

\item{scenario_id_col}{Name of the scenario identifier column added to
combined spatial outputs.}

\item{keep_routes}{Logical. Retain selected least-cost routes in each scenario
result. Use \code{TRUE} when route-density analysis is needed.}

\item{quiet}{Logical. If \code{TRUE}, suppress warnings when a scenario fails or
summary metrics are unavailable.}
}
\value{
An \code{evac_scenario_comparison} list with \code{results}, \code{summary},
\code{distance_points}, \code{time_polygons}, and \code{metadata}.
}
\description{
Runs \code{\link[=run_evacpath]{run_evacpath()}} repeatedly across named scenario overrides and returns
scenario summaries with combined spatial outputs when those layers are
available.
}
\examples{
dem <- terra::rast(nrows = 7, ncols = 7, xmin = 0, xmax = 7, ymin = 0, ymax = 7,
  vals = 1, crs = "EPSG:3857")
hazard <- terra::as.polygons(terra::crop(dem, terra::ext(1, 6, 1, 6)), dissolve = TRUE)
roads <- terra::vect(matrix(c(0, 3.5, 7, 3.5), ncol = 2, byrow = TRUE),
  type = "lines", crs = "EPSG:3857")
comparison <- compare_evac_scenarios(
  scenarios = list(baseline = list(), slower = list(walking_speed_mps = 0.9)),
  hazard_zone = hazard, roads = roads, dem = dem, grid_resolution = 1,
  road_buffer_m = 0.2, escape_buffer_m = 0.3, final_road_buffer_m = 0.2,
  max_origins = 1, max_destinations = 2, seed = 1
)
comparison$summary
}
