% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/escape_boundary.R
\name{make_road_aware_escape_zone}
\alias{make_road_aware_escape_zone}
\title{Add buffered road corridors to an escape-boundary zone}
\usage{
make_road_aware_escape_zone(
  escape_zone,
  roads,
  road_buffer_m = 2,
  crop_buffer_m = 3,
  include_base_zone = TRUE
)
}
\arguments{
\item{escape_zone}{Base escape-boundary zone, usually the land-inundation-plus-
water zone from \code{prepare_tsunami_zones()}.}

\item{roads}{Road/pathway network used to create the road-aware corridor.}

\item{road_buffer_m}{First road buffer distance in map units.}

\item{crop_buffer_m}{Optional second buffer applied before cropping/combining.}

\item{include_base_zone}{Logical. If \code{TRUE}, combine the buffered roads with
the original \code{escape_zone}. If \code{FALSE}, only the buffered road corridor is
returned.}
}
\value{
A dissolved \code{SpatVector} escape-boundary zone.
}
\description{
Creates a road-aware escape zone by combining a base escape zone with buffered
road/pathway corridors. This is useful for tsunami workflows in coastal cities
where bridges, causeways, or other walkways over water can be lost when the
inundation layer is split into land and water masks. The resulting object can
be passed to \code{find_escape_points()} so escape/safety points are generated from
a boundary that includes relevant road corridors as well as the tsunami zone.
}
\examples{
r <- terra::rast(nrows = 4, ncols = 4, xmin = 0, xmax = 4, ymin = 0, ymax = 4,
  vals = 1, crs = "EPSG:3857")
zone <- terra::as.polygons(r, dissolve = TRUE)
roads <- terra::vect(matrix(c(0, 2, 4, 2), ncol = 2, byrow = TRUE),
  type = "lines", crs = "EPSG:3857")
make_road_aware_escape_zone(zone, roads, road_buffer_m = 0.1)
}
