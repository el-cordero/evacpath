% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/planning_workflows.R
\name{map_evac_bottlenecks}
\alias{map_evac_bottlenecks}
\title{Map modeled evacuation bottlenecks}
\usage{
map_evac_bottlenecks(
  routes = NULL,
  evac_result = NULL,
  template = NULL,
  quantile_threshold = 0.9,
  min_count = NULL,
  rescale = FALSE,
  return_polygons = TRUE,
  ...
)
}
\arguments{
\item{routes}{An \code{sf} or \code{SpatVector} line layer of modeled routes.}

\item{evac_result}{Optional output from \code{\link[=run_evacpath]{run_evacpath()}} containing retained
routes.}

\item{template}{Optional \code{SpatRaster} defining the density grid. When omitted,
the function attempts to infer a template from \code{evac_result}.}

\item{quantile_threshold}{Numeric value between \code{0} and \code{1}. Used to select
high-density route cells when \code{min_count} is \code{NULL}.}

\item{min_count}{Optional numeric minimum density count. When supplied, this
takes precedence over \code{quantile_threshold}.}

\item{rescale}{Logical passed to \code{\link[leastcostpath:create_lcp_density]{leastcostpath::create_lcp_density()}}.}

\item{return_polygons}{Logical. Convert high-density raster cells to polygons.}

\item{...}{Reserved for future extensions.}
}
\value{
An \code{evac_bottleneck} list with density rasters, optional polygons,
threshold value, and summary.
}
\description{
Creates a route-density raster and identifies high-use modeled evacuation
corridors from supplied least-cost paths or an \code{\link[=run_evacpath]{run_evacpath()}} result created
with \code{keep_routes = TRUE}.
}
\examples{
template <- terra::rast(nrows = 5, ncols = 5, xmin = 0, xmax = 5, ymin = 0, ymax = 5,
  vals = 1, crs = "EPSG:3857")
routes <- sf::st_sf(
  route = c("a", "b"),
  geometry = sf::st_sfc(
    sf::st_linestring(matrix(c(0.5, 0.5, 4.5, 4.5), ncol = 2, byrow = TRUE)),
    sf::st_linestring(matrix(c(0.5, 4.5, 4.5, 0.5), ncol = 2, byrow = TRUE)),
    crs = 3857
  )
)
map_evac_bottlenecks(routes = routes, template = template)
}
