% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare.R
\name{prepare_evac_inputs}
\alias{prepare_evac_inputs}
\title{Read and project the core evacuation inputs}
\usage{
prepare_evac_inputs(
  hazard_zone,
  roads,
  dem,
  target_crs = NULL,
  hazard_as_polygon = TRUE,
  dissolve_hazard = TRUE,
  road_exclude = NULL
)
}
\arguments{
\item{hazard_zone}{Hazard/inundation zone as a \code{SpatRaster}, \code{SpatVector}, or file path.}

\item{roads}{Road/pathway network as a \code{SpatVector} or file path.}

\item{dem}{Elevation raster as a \code{SpatRaster} or file path.}

\item{target_crs}{Optional projected CRS in meters.}

\item{hazard_as_polygon}{Logical. Convert raster hazard zones to polygons.}

\item{dissolve_hazard}{Logical. Dissolve hazard polygon pieces.}

\item{road_exclude}{Optional road exclusion list passed to \code{clean_roads()}.}
}
\value{
A named list with \code{hazard_zone}, \code{roads}, and \code{dem}.
}
\description{
Read and project the core evacuation inputs
}
\examples{
dem <- terra::rast(nrows = 5, ncols = 5, xmin = 0, xmax = 5, ymin = 0, ymax = 5,
  vals = 1, crs = "EPSG:3857")
hazard <- terra::as.polygons(dem, dissolve = TRUE)
roads <- terra::vect(matrix(c(0, 2.5, 5, 2.5), ncol = 2, byrow = TRUE),
  type = "lines", crs = "EPSG:3857")
inputs <- prepare_evac_inputs(hazard, roads, dem)
names(inputs)
}
